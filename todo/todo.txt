Problem-1: remove page: parse json problem, : reques was successful, but parsing error, but why
	Reason: if res.status(204) then in frontend res.json() will failed because no, data to parse
	Solution: 
		1. send json from backend, instead of null, so that same api call works 
		2. get status from res.status, not from res.json() => data.status which is throwing error

Problem-2: Uncrontrol => controll
	Reason: we not pass default string value for optional property, so that our validation not failed for empty value is required
	Solution:
		- It is just worning, not error, 
		- ?


Show Error in UI
	1. Either set message from Alert hard coded or
	2. Set message from redux-store hard coded or
	3. Set message from backend response 							(best way)

	But Nover mixed up: 1,2,3 use either one.